# ZERO-GUESSWORK PRODUCTION HANDOFF

Authority: original source assets → latest Lucifer-approved PDF/Drive Preview → reconstructed Git master snapshot → locked contracts → browser implementation.

The PDF/Drive Preview is supreme for visible UI. Contracts exist only to remove ambiguity that images cannot express.

Claude must reconstruct both chunked archives with SHA verification before implementation, then implement route-by-route. The browser must be fixed to match the master; the master is never changed to accommodate easier code.

Goal: 100% design/section/component decision parity and zero intentional visual deviation. Pixel-level rasterization can vary by browser/font antialiasing; final GĐ7 acceptance therefore combines screenshot-diff evidence with ChatGPT visual review.
