#!/usr/bin/env python3
# Sealed copy of the pre-GD10 84-asset manifest builder. Kept only so the active wrapper can rebuild the approved baseline deterministically before applying the additive PDF-source correction.
# DO NOT invoke directly as current authority.
